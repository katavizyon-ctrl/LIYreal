<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="LIYreal creates Ankara mohair objects for body and room, beginning with Cloud & Stone: a mohair cardigan with handpicked stone buttons.">
  <title>LIYreal | Ankara Mohair Objects</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,400;0,500;1,400&family=DM+Sans:wght@300;400;500;600&display=swap" rel="stylesheet">
  <style>
    *, *::before, *::after { box-sizing: border-box; }
    * { margin: 0; }
    :root {
      --ink: #10100d;
      --ink-soft: #171611;
      --paper: #f5f0e8;
      --paper-soft: #d9cdbb;
      --honey: #c4956a;
      --clay: #9f7654;
      --moss: #71806b;
      --blue: #60727a;
      --muted: #918778;
      --line: rgba(245, 240, 232, .14);
      --line-warm: rgba(196, 149, 106, .25);
      --shadow: 0 24px 80px rgba(0, 0, 0, .35);
      --serif: "Cormorant Garamond", Georgia, serif;
      --sans: "DM Sans", Arial, sans-serif;
    }
    html { scroll-behavior: smooth; scroll-padding-top: 92px; }
    body {
      background: var(--ink);
      color: var(--paper);
      font-family: var(--sans);
      font-weight: 300;
      line-height: 1.6;
      -webkit-font-smoothing: antialiased;
      overflow-x: hidden;
    }
    img { display: block; width: 100%; }
    a { color: inherit; text-decoration: none; }
    button, input, textarea, select { font: inherit; }
    button { cursor: pointer; }
    .shell { width: min(1180px, calc(100% - 40px)); margin: 0 auto; }
    .eyebrow {
      color: var(--honey);
      font-size: .68rem;
      font-weight: 600;
      letter-spacing: .26em;
      text-transform: uppercase;
    }
    .btn {
      min-height: 48px;
      border: 1px solid var(--line-warm);
      background: transparent;
      color: var(--paper);
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: .75rem;
      padding: .85rem 1.25rem;
      font-size: .72rem;
      font-weight: 600;
      letter-spacing: .18em;
      text-transform: uppercase;
      transition: transform .25s ease, background .25s ease, border-color .25s ease, color .25s ease;
      white-space: nowrap;
    }
    .btn:hover { transform: translateY(-1px); border-color: var(--honey); }
    .btn.primary { background: var(--honey); border-color: var(--honey); color: var(--ink); }
    .btn.primary:hover { background: var(--paper); border-color: var(--paper); }

    .site-nav {
      position: fixed;
      inset: 0 0 auto;
      z-index: 20;
      min-height: 76px;
      border-bottom: 1px solid transparent;
      background: linear-gradient(to bottom, rgba(16, 16, 13, .86), rgba(16, 16, 13, .36), transparent);
      backdrop-filter: blur(12px);
      transition: background .25s ease, border-color .25s ease;
    }
    .site-nav.scrolled { background: rgba(16, 16, 13, .94); border-color: var(--line); }
    .nav-inner {
      width: min(1220px, calc(100% - 32px));
      min-height: 76px;
      margin: 0 auto;
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 1.25rem;
    }
    .brand { display: grid; gap: .12rem; }
    .brand-mark { display: flex; align-items: baseline; line-height: 1; }
    .brand-liy { font-weight: 600; letter-spacing: .28em; }
    .brand-real { font-weight: 300; letter-spacing: .12em; }
    .brand-sub {
      color: var(--honey);
      font-size: .5rem;
      font-weight: 600;
